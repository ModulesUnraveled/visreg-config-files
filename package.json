// Copy the scripts below to your package.json file
// If you alread have a "test" script, you can call this whatever you want
// e.g. `"wdio": "wdio"` would let you run `npm run wdio` instead of `npm test`
// If you rename it, update line 40 in the README.md to match

{
    "scripts": {
        "test": "wdio",
        "test:quick": "wdio wdio.conf.quick.js"
    }
}
